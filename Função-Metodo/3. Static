Método estático
Os métodos estáticos são métodos que não dependem de um objeto. Tais métodos são chamados sem que haja uma instância da classe que declara o método.
Como o método estático não possui ligação com um objeto, então o método não pode usar variáveis de instância, que são variáveis de um objeto.
Entendeu?
A principal vantagem de um método estático é que podemos chamá-lo sem criar um objeto. Ele pode acessar membros de dados estáticos e também alterar seu valor. 
É usado para criar um método de instância. É chamado usando o nome da classe. 

Exemplo de método estático
Exibir.java

public class Exibir {  
  
  static void mostrar() {  
      System.out.println ( "É um exemplo de método estático." );  
   }  
  
   public static void  main (String [] args) {  
      mostrar();  
   }  
   
}  

Saída:

É um exemplo de método estático.


sintaxes
sem retorno:
public static void jogarDados(){


}


            com retorno sem parametros:
            public static int jogarDados(){
               int n1 = 6;
               int n2 = 5;
               int soma = n1+n2;
               return soma;
            }
            
Com retorno e com parametros:
public static int jogarDados(int x, int y){
       int n1 = x;
       int n2 = y;
       int soma = n1+n2;
       return soma;
}
