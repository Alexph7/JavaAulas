Criação
LinkedList<String> nomes = new LinkedList<String>();

Metodos Específicos
nomes.addFirst("Marcelo");
nomes.addLast(Manoel);
nomes.removeFirst();
nomes.removeLast();
nomes.getFisrt();
nomes.getLast();


Iterator iter2 = nomes.iterator();
		while(iter2.hasNext()){
			System.out.println(iter2.next());
		}

